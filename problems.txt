eroor while pip install -r requirements.txt
-e is causing error , need to use while uploading it on docker
torch=2.9.1 version to be installed.

The scaler object is absolutely essential for inverse transformation because it stores the exact statistical properties (min and max values) of the original, unnormalized stock price data.
